-- flip the 'on' state
SELECT
    id, meta:name, state:on
WHERE
    id = "thing-03"
;
SET
    state:on = !state:on
WHERE
    id = "thing-03"
;
SELECT
    id, meta:name, state:on
WHERE
    id = "thing-03"
;
