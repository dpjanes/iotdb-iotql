SELECT
    id, meta:name, meta:facet
WHERE
    meta:name = "The Name"
;
SET
    meta:facet |= [ iot-facet:sensor, iot-facet:sensor.temperature ]
WHERE
    meta:name = "The Name"
;
SELECT
    id, meta:name, meta:facet
WHERE
    meta:name = "The Name"
;
