-- this will attach unit to a array of scalar 
-- this converts each individual value to the type, and 
-- attaches the type to the array as a whole
SELECT
    unit([ -40, 0, 32, 100, 212 ], iot-unit:temperature.imperial.fahrenheit)
;
