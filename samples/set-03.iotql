SET
    meta.facet = [ facets.climate, facets.climate.heating, facets.sensor, facets.sensor.temperature ]
WHERE
    meta.name = "The Name"
;
SELECT
    id, meta.name, meta.facets
WHERE
    meta.name = "The Name"
;
