SELECT
    id, meta.name,
        units(state.sensor.temperature, units.temperature.imperial.fahrenheit) AS fahrenheit,
        units(state.sensor.temperature, units.temperature.metric.celsius) AS celsius,
        units(state.sensor.temperature, units.temperature.metric.kelvin) AS kelvin
WHERE
    state.sensor.temperature = 20;
;
