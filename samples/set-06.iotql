SELECT
    id, meta.name, meta.facet
WHERE
    meta.name = "The Name"
;
SET
    meta.facet &= [ facets.sensor, facets.sensor.temperature ]
WHERE
    meta.name = "The Name"
;
SELECT
    id, meta.name, meta.facet
WHERE
    meta.name = "The Name"
;
