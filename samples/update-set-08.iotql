-- this will set all the subsets of the facet too
UPDATE
    things
SET
    meta:facet = iot-facet:sensor.chemical.co2
WHERE
    meta:name = "The Name"
;
SELECT
    id, meta:name, meta:facet
WHERE
    meta:name = "The Name"
;
